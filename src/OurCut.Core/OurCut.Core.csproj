<Project Sdk="Microsoft.NET.Sdk">
  <!-- Project model, timeline and edit commands. No UI or media dependencies,
       so the same operations can be driven by the app or a future MCP server. -->
  <PropertyGroup>
    <RootNamespace>OurCut.Core</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="OurCut.Core.Tests" />
  </ItemGroup>
</Project>

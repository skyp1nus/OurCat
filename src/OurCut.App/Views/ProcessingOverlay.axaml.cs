using Avalonia.Controls;

namespace OurCut.App.Views;

/// <summary>Covers the editor below the title bar while a file is prepared (<see cref="ViewModels.ProcessingViewModel"/>).</summary>
public partial class ProcessingOverlay : UserControl
{
    public ProcessingOverlay() => InitializeComponent();
}
